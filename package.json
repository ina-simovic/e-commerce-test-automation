{
  "name": "e-commerce-test-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "npx rimraf ./allure-results/ && npx rimraf ./allure-report/",
    "test": "npm run clean && npx playwright test",
    "test:headed": "npm run clean && npx playwright test --headed",
    "test@cart": "npm run clean && npx playwright test --grep @cart",
    "pretty": "npx prettier . --write",
    "test:headed:single-worker": "npm run clean && npx playwright test --headed --workers 1",
    "report:generate": "npx allure generate",
    "report:generate-open": "npx allure generate && npx allure open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.3.2",
    "allure": "^3.3.1",
    "allure-playwright": "^3.5.0",
    "prettier": "^3.8.1",
    "rimraf": "^6.1.3"
  }
}
